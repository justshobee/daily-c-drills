# ./03-reverse_string.txt

**Assignment name:** reverse_string

**Expected files:** reverse_string.c

**Allowed functions:** write

Write a program that reverses the first argument string and prints the reversed string followed by a newline. If no argument, print newline.

```
$> ./reverse_string "Hello" | cat -e
olleH$
```
