# rle_decode

**Assignment name :** rle_decode  
**Expected files :** rle_decode.c  
**Allowed functions:** write

---

Write a program that decodes a run-length encoded string (like 'a3b2c1') and prints the original string followed by newline.

```
$> ./rle_decode 'a3b2c1' | cat -e
aaabbc$
```
