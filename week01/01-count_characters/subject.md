# 01-count_characters

**Assignment name:** count_characters  
**Expected files:** count_characters.c  
**Allowed functions:** write

Write a program that counts the number of characters in the first argument string and prints the count followed by a newline. If no argument, print 0 and newline.

```
$> ./count_characters "Hello, World!" | cat -e
13$
```
