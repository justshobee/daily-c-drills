# rotate_right

**Assignment name :** rotate_right  
**Expected files :** rotate_right.c  
**Allowed functions:** write

---

Write a program that rotates the characters of the first argument right by N positions (second arg) and prints the result followed by newline.

```
$> ./rotate_right 'abcdef' 2 | cat -e
efabcd$
```